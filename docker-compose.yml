version: '3.8'

services:
  db:
    image: postgres:15
    container_name: pg-docker
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./pgdata:/var/lib/postgresql/data
    ports:
      - "5434:5432"